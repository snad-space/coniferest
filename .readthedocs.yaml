version: 2

build:
  os: "ubuntu-20.04"
  tools:
    python: "3.11"
  apt_packages:
    # For the iTree visualization notebook
    - graphviz

formats: all

sphinx:
   configuration: docs/conf.py

python:
  install:
    - requirements: docs/requirements.txt
    - method: pip
      path: .
